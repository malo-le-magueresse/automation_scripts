### DATA URL
URL = "weather_data_url"

#### CONSTANTS
DB_PATH = '~/Library/Messages/chat.db' # The file where iMessages are stored
RECIPIENT_NUMBERS = ['phone_numbers'] # The numbers we want to automate the sending of messages to
SCRIPT_PATH = "path to the AppleScript file" # The path to the AppleScript file that actually sends the message
